## CONTEXTO DEL PROYECTO — Leer antes de escribir cualquier línea

Lee estos archivos en orden antes de escribir cualquier código:
1. `.windsurf/backend.md` → estructura del backend, mapa de imports,
   endpoints registrados
2. `.windsurf/frontend.md` → componentes Svelte, cómo consumir la API,
   variables de entorno

Mapa de imports válidos — NO inferir, usar exactamente estos:

| Qué necesitas importar | Desde dónde importarlo   |
|------------------------|--------------------------|
| `get_db`               | `app.db.session`         |
| `AsyncSession`         | `sqlalchemy.ext.asyncio` |
| `Base`                 | `app.models.base`        |
| `upload_image`         | `app.core.cloudinary`    |
| `settings`             | `app.core.config`        |

Stack frontend: Astro v6 + Svelte 5 + TypeScript + Tailwind CSS +
Lucide Svelte. NO usar React, hooks de React ni JSX en ningún archivo
frontend.

---

## PASO 0 — INSPECCIÓN TOTAL (obligatorio, no escribir código hasta completarla)

Leer en este orden y construir la tabla antes de continuar:

1. `frontend/src/components/` → por cada componente Svelte anotar:
   - qué datos renderiza (props, variables, textos hardcodeados)
   - si tiene imágenes, iconos Lucide, colores, listas

2. `backend/app/models/` → campos exactos de cada modelo existente

3. `backend/app/schemas/` → campos exactos de cada schema existente

4. `backend/app/api/v1/endpoints/` → por cada router anotar:
   - método HTTP y path exacto CON slash final
   - si usa Form(...), File(...), Path param

5. `frontend/src/components/admin/` → si existe, listar editores ya creados

6. `frontend/src/lib/` → si existe config.ts o api.ts, leer su contenido

7. `frontend/tailwind.config.js` → leer el array `content` y verificar
   que cubre `./src/**/*.{astro,html,js,ts,svelte}`

Presentar esta tabla y esperar confirmación antes de continuar:

| Dominio | Campos del componente | Path GET | Path POST | Path PUT | Path DELETE |
|---------|-----------------------|----------|-----------|----------|-------------|

Regla: los campos del componente Svelte mandan.
El modelo DB debe reflejarlos exactamente, no al revés.
Si hay discrepancia entre el componente y el modelo existente,
notificarlo y preguntar antes de modificar el modelo.

---

## FASE 1 — Backend: CORS en `backend/app/main.py`

Verificar que existe este middleware. Si no está, agregarlo sin borrar
nada del archivo:
```python
from fastapi.middleware.cors import CORSMiddleware

app.add_middleware(
    CORSMiddleware,
    allow_origins=["http://localhost:4321"],
    allow_credentials=True,
    allow_methods=["*"],
    allow_headers=["*"],
)
```

---

## FASE 2 — Frontend: Tailwind (`frontend/tailwind.config.js`)

Verificar que el array `content` cubre todos los archivos del proyecto.
Si no lo hace, actualizarlo sin borrar configuración existente:
```javascript
content: [
  './src/**/*.{astro,html,js,ts,jsx,tsx,svelte}',
],
```

Esta verificación es obligatoria antes de crear cualquier layout o
componente admin — si Tailwind no escanea los archivos correctos,
ningún estilo aparecerá en el panel admin.

---

## FASE 3 — Frontend: Config (`frontend/src/lib/config.ts`)

Crear si no existe. Si existe, verificar que tiene este contenido exacto:
```typescript
const API_BASE_URL = import.meta.env.PUBLIC_API_URL;

export async function fetchApi<T>(
  endpoint: string,
  options?: RequestInit
): Promise<T> {
  const url = `${API_BASE_URL}${endpoint}`;

  const config: RequestInit = {
    ...options,
    headers: { ...options?.headers },
  };

  if (!(options?.body instanceof FormData)) {
    config.headers = {
      ...config.headers,
      "Content-Type": "application/json",
    };
  }

  const response = await fetch(url, config);

  if (!response.ok) {
    let errorMessage = "Error desconocido del servidor";
    try {
      const errorData = await response.json();
      errorMessage = errorData.detail || errorMessage;
    } catch {
      // usar mensaje por defecto
    }
    throw new Error(errorMessage);
  }

  return response.json() as Promise<T>;
}
```

Agregar al `frontend/.env` si no está:
```env
PUBLIC_API_URL=http://localhost:8000
```

---

## FASE 4 — Frontend: Tipos TypeScript (`frontend/src/lib/types.ts`)

Crear un interface por cada dominio que refleje exactamente los campos
del `{Dominio}Response` del backend:
```typescript
export interface HeroResponse {
  id: number;
  // campos exactos del schema HeroResponse
  image_url: string | null;
}
// repetir por cada dominio — sin mezclar interfaces
```

---

## FASE 5 — Frontend: Layout del panel admin

### 5.1 Crear `frontend/src/layouts/AdminLayout.astro`

Este layout envuelve TODAS las páginas de `/admin/`.
Colores del portfolio: amber (#fbbf24), orange (#fb923c), zinc (#18181b).

CRÍTICO: importar el CSS global exactamente igual que en `Layout.astro`
principal — leer ese archivo primero y copiar el mismo patrón de import.
```astro
---
import AdminSidebar from '../components/admin/AdminSidebar.svelte';
import '../styles/global.css';

interface Props {
  title: string;
  currentPath?: string;
}

const { title, currentPath = '' } = Astro.props;
---

<!DOCTYPE html>
<html lang="es">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>{title} — Admin</title>
    <link rel="preconnect" href="https://fonts.googleapis.com" />
    <link
      href="https://fonts.googleapis.com/css2?family=Inter:wght@300;400;500;600;700&display=swap"
      rel="stylesheet"
    />
  </head>
  <body class="bg-zinc-950 text-zinc-100 font-sans">
    <div class="flex min-h-screen">
      <AdminSidebar client:load currentPath={currentPath} />
      <main class="flex-1 p-6 md:p-10 transition-all duration-300">
        <slot />
      </main>
    </div>
  </body>
</html>
```

### 5.2 Crear `frontend/src/components/admin/AdminSidebar.svelte`

Usar EXACTAMENTE este código — no modificar la estructura de elementos:
```svelte
<script lang="ts">
  import { onMount } from 'svelte';
  import {
    LayoutDashboard,
    Sparkles,
    User,
    Code2,
    FolderKanban,
    Heart,
    ChevronLeft,
    ChevronRight,
    ExternalLink,
  } from 'lucide-svelte';

  export let currentPath: string = '';

  let collapsed = false;

  onMount(() => {
    collapsed = window.innerWidth < 768;
    window.addEventListener('resize', () => {
      if (window.innerWidth < 768) collapsed = true;
    });
  });

  function toggle() {
    collapsed = !collapsed;
  }

  const navItems = [
    { href: '/admin',          label: 'Dashboard', icon: LayoutDashboard },
    { href: '/admin/hero',     label: 'Hero',      icon: Sparkles        },
    { href: '/admin/about',    label: 'About',     icon: User            },
    { href: '/admin/stack',    label: 'Stack',     icon: Code2           },
    { href: '/admin/projects', label: 'Proyectos', icon: FolderKanban    },
    { href: '/admin/passions', label: 'Pasiones',  icon: Heart           },
  ];

  function isActive(href: string): boolean {
    if (href === '/admin') return currentPath === '/admin';
    return currentPath.startsWith(href);
  }
</script>

<aside
  class="relative flex flex-col bg-zinc-900 border-r border-zinc-800
         transition-all duration-300 min-h-screen
         {collapsed ? 'w-16' : 'w-60'}"
>
  <!-- Botón toggle -->
  <button
    on:click={toggle}
    class="absolute -right-3 top-6 z-10 bg-zinc-800 border border-zinc-700
           rounded-full p-1 text-zinc-400 hover:text-amber-400
           hover:border-amber-400 transition-colors duration-200"
    aria-label={collapsed ? 'Expandir menú' : 'Colapsar menú'}
  >
    {#if collapsed}
      <ChevronRight size={14} />
    {:else}
      <ChevronLeft size={14} />
    {/if}
  </button>

  <!-- Logo -->
  <div class="flex items-center gap-3 px-4 py-5 border-b border-zinc-800">
    <div class="w-8 h-8 rounded-lg flex-shrink-0 bg-gradient-to-br
                from-amber-400 to-orange-500 flex items-center
                justify-center text-zinc-900 font-bold text-sm">
      H
    </div>
    {#if !collapsed}
      <div class="overflow-hidden">
        <p class="text-sm font-semibold text-zinc-100 truncate">
          El Rincón de Harco
        </p>
        <p class="text-xs text-zinc-500">Panel admin</p>
      </div>
    {/if}
  </div>

  <!-- Navegación -->
  <nav class="flex-1 py-4 space-y-1 px-2">
    {#each navItems as item}
      {@const active = isActive(item.href)}
      <div class="relative group">
        
          href={item.href}
          class="flex items-center gap-3 px-2 py-2.5 rounded-lg
                 transition-all duration-200 border-l-2
                 {active
                   ? 'bg-zinc-800 text-amber-400 border-amber-400'
                   : 'text-zinc-400 hover:bg-zinc-800 hover:text-amber-400 border-transparent'}
                 {collapsed ? 'justify-center' : ''}"
        >
          <svelte:component this={item.icon} size={18} class="flex-shrink-0" />
          {#if !collapsed}
            <span class="text-sm font-medium truncate">{item.label}</span>
          {/if}
        </a>
        {#if collapsed}
          <span class="absolute left-full top-1/2 -translate-y-1/2 ml-2
                       px-2 py-1 rounded-md bg-zinc-800 text-zinc-100
                       text-xs whitespace-nowrap opacity-0
                       group-hover:opacity-100 pointer-events-none
                       transition-opacity duration-150 z-50">
            {item.label}
          </span>
        {/if}
      </div>
    {/each}
  </nav>

  <!-- Volver al sitio -->
  <div class="px-2 py-4 border-t border-zinc-800">
    <div class="relative group">
      
        href="/"
        class="flex items-center gap-3 px-2 py-2.5 rounded-lg
               text-zinc-400 hover:bg-zinc-800 hover:text-amber-400
               transition-all duration-200
               {collapsed ? 'justify-center' : ''}"
      >
        <ExternalLink size={18} class="flex-shrink-0" />
        {#if !collapsed}
          <span class="text-sm font-medium">Volver al sitio</span>
        {/if}
      </a>
      {#if collapsed}
        <span class="absolute left-full top-1/2 -translate-y-1/2 ml-2
                     px-2 py-1 rounded-md bg-zinc-800 text-zinc-100
                     text-xs whitespace-nowrap opacity-0
                     group-hover:opacity-100 pointer-events-none
                     transition-opacity duration-150 z-50">
          Volver al sitio
        </span>
      {/if}
    </div>
  </div>
</aside>
```

---

## FASE 6 — Frontend: Páginas admin

Estructura de carpetas a crear:
```
frontend/src/pages/admin/
├── index.astro
├── hero.astro
├── about.astro
├── stack.astro
├── projects.astro
└── passions.astro
```

Patrón para cada página individual:
```astro
---
import AdminLayout from '../../layouts/AdminLayout.astro';
import HeroEditor from '../../components/admin/HeroEditor.svelte';

const currentPath = Astro.url.pathname;
---

<AdminLayout title="Hero" currentPath={currentPath}>
  <div class="max-w-2xl">
    <h1 class="text-2xl font-bold text-zinc-100 mb-8">Editar Hero</h1>
    <HeroEditor client:load />
  </div>
</AdminLayout>
```

Dashboard `index.astro` — las cards van en un componente Svelte separado
para evitar errores de parsing en Astro:
```astro
---
import AdminLayout from '../../layouts/AdminLayout.astro';
import DashboardCards from '../../components/admin/DashboardCards.svelte';

const currentPath = Astro.url.pathname;
---

<AdminLayout title="Dashboard" currentPath={currentPath}>
  <div>
    <h1 class="text-2xl font-bold text-zinc-100 mb-2">Dashboard</h1>
    <p class="text-zinc-400 mb-8">
      Gestiona el contenido de tu portfolio desde aquí.
    </p>
    <DashboardCards client:load />
  </div>
</AdminLayout>
```

Crear `frontend/src/components/admin/DashboardCards.svelte`:
```svelte
<script lang="ts">
  const sections = [
    { href: '/admin/hero',     label: 'Hero',      description: 'Título y subtítulo principal' },
    { href: '/admin/about',    label: 'About',     description: 'Sección sobre mí'             },
    { href: '/admin/stack',    label: 'Stack',     description: 'Tecnologías y herramientas'   },
    { href: '/admin/projects', label: 'Proyectos', description: 'Galería de proyectos'         },
    { href: '/admin/passions', label: 'Pasiones',  description: 'Sección de pasiones'          },
  ];
</script>

<div class="grid grid-cols-1 sm:grid-cols-2 lg:grid-cols-3 gap-4">
  {#each sections as section}
    
      href={section.href}
      class="block p-5 rounded-xl bg-zinc-900 border border-zinc-800
             hover:border-amber-400 hover:bg-zinc-800
             transition-all duration-200 group"
    >
      <h2 class="text-base font-semibold text-zinc-100
                 group-hover:text-amber-400 mb-1 transition-colors">
        {section.label}
      </h2>
      <p class="text-sm text-zinc-500">{section.description}</p>
    </a>
  {/each}
</div>
```

---

## FASE 7 — Frontend: Editores por dominio

Crear un componente Svelte por cada dominio en
`frontend/src/components/admin/`.

Patrón para sección única (hero, about — PUT sobre registro existente):
```svelte
<script lang="ts">
  import { onMount } from 'svelte';
  import { fetchApi } from '$lib/config';
  import type { HeroResponse } from '$lib/types';

  const API = import.meta.env.PUBLIC_API_URL;

  let data: HeroResponse | null = null;
  let loading = true;
  let saving = false;
  let message = '';
  let messageType: 'success' | 'error' = 'success';

  let title = '';
  let subtitle = '';
  let imageFile: File | null = null;

  onMount(async () => {
    try {
      data = await fetchApi<HeroResponse>('/api/v1/heroes/latest/');
      title    = data.title    ?? '';
      subtitle = data.subtitle ?? '';
    } catch {
      message     = 'Error al cargar los datos';
      messageType = 'error';
    } finally {
      loading = false;
    }
  });

  async function handleSubmit() {
    if (!data) return;
    saving  = true;
    message = '';

    try {
      const formData = new FormData();
      formData.append('title', title);
      formData.append('subtitle', subtitle);
      if (imageFile) formData.append('image', imageFile);
      // NO agregar Content-Type — el navegador lo genera automáticamente

      const res = await fetch(`${API}/api/v1/heroes/${data.id}`, {
        method: 'PUT',
        body: formData,
      });

      if (!res.ok) throw new Error();
      data        = await res.json();
      message     = 'Guardado correctamente';
      messageType = 'success';
    } catch {
      message     = 'Error al guardar los cambios';
      messageType = 'error';
    } finally {
      saving = false;
    }
  }
</script>

{#if loading}
  <div class="flex items-center gap-2 text-zinc-400">
    <span class="animate-spin inline-block w-4 h-4 border-2
                 border-zinc-600 border-t-amber-400 rounded-full"></span>
    <span class="text-sm">Cargando...</span>
  </div>
{:else}
  <form on:submit|preventDefault={handleSubmit} class="space-y-6">

    <div>
      <label class="block text-sm font-medium text-zinc-300 mb-2">
        Imagen
      </label>
      {#if data?.image_url}
        <img
          src={data.image_url}
          alt="Imagen actual"
          class="w-32 h-32 object-cover rounded-lg mb-3 border border-zinc-700"
        />
      {/if}
      <input
        type="file"
        accept="image/*"
        on:change={(e) => {
          const input = e.target as HTMLInputElement;
          imageFile = input.files?.[0] ?? null;
        }}
        class="block w-full text-sm text-zinc-400
               file:mr-4 file:py-2 file:px-4 file:rounded-lg
               file:border-0 file:bg-zinc-800 file:text-amber-400
               hover:file:bg-zinc-700 cursor-pointer"
      />
      <p class="text-xs text-zinc-500 mt-1">
        Deja vacío para mantener la imagen actual
      </p>
    </div>

    <div>
      <label class="block text-sm font-medium text-zinc-300 mb-2">
        Título
      </label>
      <input
        type="text"
        bind:value={title}
        required
        class="w-full px-4 py-2.5 rounded-lg bg-zinc-800 border border-zinc-700
               text-zinc-100 placeholder-zinc-500 focus:outline-none
               focus:border-amber-400 focus:ring-1 focus:ring-amber-400
               transition-colors"
        placeholder="Título principal"
      />
    </div>

    <div>
      <label class="block text-sm font-medium text-zinc-300 mb-2">
        Subtítulo
      </label>
      <textarea
        bind:value={subtitle}
        rows={4}
        class="w-full px-4 py-2.5 rounded-lg bg-zinc-800 border border-zinc-700
               text-zinc-100 placeholder-zinc-500 resize-none focus:outline-none
               focus:border-amber-400 focus:ring-1 focus:ring-amber-400
               transition-colors"
        placeholder="Subtítulo o descripción"
      ></textarea>
    </div>

    <div class="flex items-center gap-4">
      <button
        type="submit"
        disabled={saving}
        class="px-6 py-2.5 rounded-lg font-medium text-sm
               bg-gradient-to-r from-amber-400 to-orange-500
               text-zinc-900 hover:from-amber-300 hover:to-orange-400
               disabled:opacity-50 disabled:cursor-not-allowed
               transition-all duration-200"
      >
        {saving ? 'Guardando...' : 'Guardar cambios'}
      </button>

      {#if message}
        <span class="text-sm {messageType === 'success'
          ? 'text-emerald-400' : 'text-red-400'}">
          {message}
        </span>
      {/if}
    </div>
  </form>
{/if}
```

Patrón para sección múltiple (stack, projects, passions):
- Lista de items con botón editar y botón eliminar
- Al editar: poblar formulario con datos del item y setear `editingId`
- Al guardar: si `editingId !== null` → PUT, si no → POST
- DELETE con `confirm()` antes de eliminar
- Después de guardar: refrescar lista con GET y limpiar formulario

---

## FASE 8 — Frontend: Conectar componentes públicos

Patrón sección única (GET /latest/):
```svelte
<script lang="ts">
  import { onMount } from 'svelte';
  import { fetchApi } from '$lib/config';
  import type { HeroResponse } from '$lib/types';

  let data: HeroResponse | null = null;
  let loading = true;

  onMount(async () => {
    try {
      data = await fetchApi<HeroResponse>('/api/v1/heroes/latest/');
    } catch {
      // null — el template maneja el estado vacío
    } finally {
      loading = false;
    }
  });
</script>

{#if loading}
  <!-- spinner mínimo -->
{:else if data}
  <!-- mismo HTML y Tailwind original — solo valores → data.campo -->
{/if}
```

Patrón sección múltiple (GET /):
```svelte
<script lang="ts">
  import { onMount } from 'svelte';
  import { fetchApi } from '$lib/config';
  import type { StackResponse } from '$lib/types';

  let items: StackResponse[] = [];
  let loading = true;

  onMount(async () => {
    try {
      items = await fetchApi<StackResponse[]>('/api/v1/stacks/');
    } catch {
      items = [];
    } finally {
      loading = false;
    }
  });
</script>

{#each items as item}
  <!-- mismo HTML y Tailwind original — solo valores → item.campo -->
{/each}
```

---

## Reglas absolutas — Backend

- ❌ Nunca usar `from app.core.config import get_db` —
  `get_db` vive en `app.db.session`
- ❌ Nunca inferir paths de imports — usar solo el mapa del inicio
- ❌ Nunca usar `modelo: ModeloCreate` directo en POST/PUT —
  siempre `Depends(get_{dominio}_form)`
- ❌ Nunca guardar binario de imagen — solo URL de `upload_image()`
- ❌ Nunca eliminar routers existentes en `route.py`
- ✅ Si en PUT no llega imagen nueva, conservar `image_url` actual en DB

---

## Reglas absolutas — Archivos `.astro`

- ❌ Nunca usar `.map()` con arrow functions que contengan JSX/HTML
  directamente en el template de un `.astro` — el parser falla
- ✅ Todo contenido dinámico con listas va en componentes `.svelte`
  cargados con `client:load` — nunca inline en `.astro`
- ❌ Nunca importar Lucide Svelte en archivos `.astro` —
  solo funciona dentro de `.svelte`
- ✅ Para iconos en `.astro` usar SVG inline
- ❌ Nunca usar expresiones ternarias complejas en atributos `class`
  de templates `.astro` — calcular la clase en el frontmatter
- ✅ El import del CSS global en `AdminLayout.astro` debe ser idéntico
  al del `Layout.astro` principal — leer ese archivo primero

---

## Reglas absolutas — Componentes Svelte 5

- ❌ Nunca abrir un `{#if}` o `{#each}` dentro de un elemento HTML
  y cerrarlo fuera — Svelte 5 es estricto con el anidamiento
- ❌ Nunca poner el tooltip como hijo del `<a>` con `{#if}` cruzando
  el cierre del `<a>`
- ✅ Patrón correcto para links con tooltip en modo colapsado:
```svelte
  <div class="relative group">
    <a href="...">
      <!-- icono y texto -->
    </a>
    {#if collapsed}
      <span class="absolute left-full ...">Tooltip</span>
    {/if}
  </div>
```
  El tooltip es HERMANO del `<a>`, nunca hijo
- ❌ Nunca usar `<svelte:component this={undefined}>` —
  verificar que el componente existe antes de renderizarlo
- ❌ Nunca omitir el tag de apertura `<a` al refactorizar —
  verificar que cada `href=` tiene su `<a` correspondiente antes
  de escribir el archivo
- ✅ Después de escribir cualquier bloque con `{#each}` o `{#if}`,
  contar que cada tag de apertura tiene su cierre correspondiente

---

## Reglas absolutas — Tailwind y estilos

- ✅ Verificar `tailwind.config.js` en el PASO 0 — el array `content`
  debe cubrir `./src/**/*.{astro,html,js,ts,svelte}`
- ✅ `AdminLayout.astro` debe importar el CSS global igual que
  `Layout.astro` — leer el layout principal y copiar el mismo patrón
- ❌ Nunca crear un layout sin importar los estilos globales —
  sin ese import Tailwind no genera las clases para esas páginas
- ❌ Nunca usar `font-[Inter]` en clases Tailwind sin verificar que
  la fuente está cargada — usar `font-sans` como fallback seguro

---

## Reglas absolutas — Frontend general

- ❌ Nunca agregar `Content-Type: multipart/form-data` manualmente —
  el navegador lo genera con el boundary correcto automáticamente
- ❌ Nunca enviar imagen como base64 ni JSON — siempre `FormData`
- ❌ Nunca hardcodear URLs del API fuera del `.env` —
  siempre `import.meta.env.PUBLIC_API_URL`
- ❌ Nunca usar `<form>` de Astro con method POST hacia el backend —
  siempre `fetch` desde componentes Svelte con `client:load`
- ✅ `image_url` null → usar `''` o imagen placeholder, nunca romper
- ✅ El PASO 0 es obligatorio — no escribir ningún archivo hasta que
  la tabla esté completa y confirmada
- ✅ Ante cualquier discrepancia entre componente y modelo, preguntar
  antes de modificar
